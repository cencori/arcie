# URL of the arcie dev / production HTTP server. The Next.js server route
# proxies chat messages to this URL. Keep this server-side only (no NEXT_PUBLIC_
# prefix) so the browser never talks to arcie directly.
ARCIE_URL=http://localhost:3000
