# Absolute path to the agent directory this app talks to.
# `arcie dev` sets this automatically when running locally.
# In production, set it to point at the agent files bundled with your deploy.
# ARCIE_AGENT_DIR=/absolute/path/to/agent
